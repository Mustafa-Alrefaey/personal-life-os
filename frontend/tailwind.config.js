/** @type {import('tailwindcss').Config} */
// Note: In Tailwind v4, content scanning is automatic and darkMode
// is configured via @variant dark in CSS — not in this config file.
export default {
  theme: {
    extend: {},
  },
  plugins: [],
}
